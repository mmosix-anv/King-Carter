# Implementation Plan

- [x] 1. Write bug condition exploration test
  - **Property 1: Fault Condition** - Homepage Sections Display Incorrect Layout and Spacing
  - **CRITICAL**: This test MUST FAIL on unfixed code - failure confirms the bug exists
  - **DO NOT attempt to fix the test or the code when it fails**
  - **NOTE**: This test encodes the expected behavior - it will validate the fix when it passes after implementation
  - **GOAL**: Surface counterexamples that demonstrate the styling bugs exist
  - **Scoped PBT Approach**: Scope the property to the concrete failing cases - "Why King & Carter" section with carousel/incorrect padding/absolute positioning, and "Who We Are" section with excessive padding/manual breaks/incorrect margins
  - Test that "Why King & Carter" section displays carousel navigation (should be flexbox row)
  - Test that "Why King & Carter" section has padding "106px 330px 139px" (should be consistent centered padding)
  - Test that "Why King & Carter" subtitle has left margin "243px" (should be center-aligned)
  - Test that "Why King & Carter" feature text uses absolute positioning with "left: 52px" (should use flexbox)
  - Test that "Who We Are" section has padding "135px 208px 91px" (should match reference design)
  - Test that "Who We Are" text contains manual line breaks (should flow naturally)
  - Test that "Who We Are" text elements have "29px" margins (should match reference spacing)
  - Run test on UNFIXED code
  - **EXPECTED OUTCOME**: Test FAILS (this is correct - it proves the bugs exist)
  - Document counterexamples found: carousel component present, hardcoded padding/positioning values, manual breaks in text
  - Mark task complete when test is written, run, and failures are documented
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.5, 1.6, 1.7_

- [x] 2. Write preservation property tests (BEFORE implementing fix)
  - **Property 2: Preservation** - Non-Styling Functionality and Content
  - **IMPORTANT**: Follow observation-first methodology
  - Observe behavior on UNFIXED code for non-buggy contexts (mobile responsive behavior, content display, button functionality)
  - Observe: "Why King & Carter" items wrap responsively on mobile viewports
  - Observe: All section titles, subtitles, and feature items display with correct text content
  - Observe: "Learn More About Us" button maintains hover effects and click functionality
  - Observe: Inter font family and color scheme are consistent across sections
  - Observe: "Who We Are" section stacks content vertically on mobile devices
  - Write property-based tests capturing observed behavior patterns from Preservation Requirements
  - Test mobile responsive wrapping for "Why King & Carter" items across viewport range
  - Test that all text content remains displayed (titles, subtitles, body text, feature items)
  - Test button hover effects and functionality in "Who We Are" section
  - Test font family (Inter) and color scheme consistency
  - Test mobile vertical stacking in "Who We Are" section
  - Property-based testing generates many test cases for stronger guarantees
  - Run tests on UNFIXED code
  - **EXPECTED OUTCOME**: Tests PASS (this confirms baseline behavior to preserve)
  - Mark task complete when tests are written, run, and passing on unfixed code
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 3.6, 3.7_

- [x] 3. Fix homepage sections styling

  - [x] 3.1 Replace carousel with flexbox row in "Why King & Carter"
    - Remove carousel component/logic from "Why King & Carter" section
    - Replace with flex container: display: flex, flex-direction: row, justify-content: center
    - Ensure all 5 feature items render as direct children
    - Add flex-wrap: wrap for responsive behavior
    - _Bug_Condition: isBugCondition(input) where input.sectionName == "Why King & Carter" AND input.layout == "carousel"_
    - _Expected_Behavior: All 5 items displayed in horizontal row without carousel navigation_
    - _Preservation: Mobile responsive wrapping must continue to work (Requirement 3.1)_
    - _Requirements: 2.1_

  - [x] 3.2 Normalize padding values in both sections
    - Replace "Why King & Carter" padding "106px 330px 139px" with consistent values matching reference design
    - Replace "Who We Are" padding "135px 208px 91px" with consistent values matching reference proportions
    - Use design system tokens or consistent spacing (e.g., 80px 40px)
    - _Bug_Condition: isBugCondition(input) where input.padding == "106px 330px 139px" OR input.padding == "135px 208px 91px"_
    - _Expected_Behavior: Consistent, centered padding matching reference design_
    - _Preservation: Content display and layout structure must remain unchanged (Requirements 3.2, 3.4)_
    - _Requirements: 2.2, 2.5_

  - [x] 3.3 Remove absolute positioning from subtitle and feature text
    - Remove left: 243px positioning from "Why King & Carter" subtitle
    - Use text-align: center or flexbox centering on parent container
    - Remove position: absolute and left: 52px from feature text
    - Use flexbox properties for spacing (gap, padding, or margin)
    - Ensure elements are in normal document flow
    - _Bug_Condition: isBugCondition(input) where input.subtitlePosition.left == "243px" OR input.featureTextPosition.left == "52px"_
    - _Expected_Behavior: Center-aligned subtitle and flexbox layout for feature items without absolute positioning_
    - _Preservation: Text content and feature items must continue to display correctly (Requirement 3.3)_
    - _Requirements: 2.3, 2.4_

  - [x] 3.4 Remove manual line breaks from "Who We Are" text
    - Remove <br> tags or \n characters from text strings in "Who We Are" section
    - Allow text to wrap naturally based on container width
    - Ensure paragraph elements handle text flow
    - _Bug_Condition: isBugCondition(input) where input.sectionName == "Who We Are" AND input.textContainsManualBreaks == true_
    - _Expected_Behavior: Text flows naturally without forced line breaks_
    - _Preservation: Text content must remain displayed correctly (Requirement 3.4)_
    - _Requirements: 2.6_

  - [x] 3.5 Update text margins in "Who We Are" section
    - Replace margin: 29px with values matching reference design
    - Use consistent spacing tokens (e.g., margin-bottom: 24px or 1.5rem)
    - Apply correct spacing between text elements
    - _Bug_Condition: isBugCondition(input) where input.sectionName == "Who We Are" AND input.textMargin == "29px"_
    - _Expected_Behavior: Spacing between text elements matches reference design_
    - _Preservation: Text content and layout structure must remain unchanged (Requirement 3.4)_
    - _Requirements: 2.7_

  - [x] 3.6 Verify bug condition exploration test now passes
    - **Property 1: Expected Behavior** - Homepage Sections Display Correct Layout and Spacing
    - **IMPORTANT**: Re-run the SAME test from task 1 - do NOT write a new test
    - The test from task 1 encodes the expected behavior
    - When this test passes, it confirms the expected behavior is satisfied
    - Run bug condition exploration test from step 1
    - Verify "Why King & Carter" displays all 5 items in flexbox row without carousel
    - Verify both sections have consistent padding matching reference design
    - Verify subtitle is center-aligned without fixed margins
    - Verify feature items use flexbox layout without absolute positioning
    - Verify "Who We Are" text flows naturally without manual breaks
    - Verify text margins match reference spacing
    - **EXPECTED OUTCOME**: Test PASSES (confirms bugs are fixed)
    - _Requirements: 2.1, 2.2, 2.3, 2.4, 2.5, 2.6, 2.7_

  - [x] 3.7 Verify preservation tests still pass
    - **Property 2: Preservation** - Non-Styling Functionality and Content
    - **IMPORTANT**: Re-run the SAME tests from task 2 - do NOT write new tests
    - Run preservation property tests from step 2
    - Verify mobile responsive behavior continues to work (items wrap, content stacks)
    - Verify all text content remains displayed correctly
    - Verify button functionality and hover effects work
    - Verify font family and color scheme remain consistent
    - **EXPECTED OUTCOME**: Tests PASS (confirms no regressions)
    - Confirm all tests still pass after fix (no regressions)
    - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 3.6, 3.7_

- [x] 4. Checkpoint - Ensure all tests pass
  - Ensure all tests pass, ask the user if questions arise.
